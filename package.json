{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Placeholder for Render deployment",
  "scripts": {
    "start": "echo 'No Node.js server running' && exit 0"
  },
  "engines": {
    "node": "18"
  }
}